# bare name (no slash) so a worktree node_modules *symlink* is ignored too, not just the dir
node_modules

# Build output
dist/
out/

# Logs / OS cruft
*.log
.DS_Store

# Local Claude Code state (settings, plans, worktrees) — nothing here is shipped
.claude/
